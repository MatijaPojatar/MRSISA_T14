// import Vue from "vue";

const initStanje = () => {
  return {
    
  }
}

const state = initStanje();

const getters = {
 
}

const actions = {
  

}

const mutations = {
 
}

export default { state, mutations, actions, getters, namespaced: true}